## -----------------------------------------------------------------------------
	library('sowreap');
	f = function(n) {
		if (n == 0) return();
		if ((n %% 2) == 1) Sow(n);
		f(n - 1)
	}
	r = Reap(f(10));
	print(r);

